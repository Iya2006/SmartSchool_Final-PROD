# ============================================
# SMARTSCHOOL ERP — Dépendances Python
# ============================================

# Framework API
fastapi
uvicorn[standard]

# ORM & Base de données
sqlalchemy
pg8000
alembic

# Validation
pydantic
pydantic-settings

# Authentification & Sécurité
PyJWT
passlib
bcrypt==3.2.2

# Environnement
python-dotenv

# Upload fichiers
python-multipart

# Lecture des classeurs Excel téléversés (import des résultats d'examen
# national). Optionnelle au sens strict : sans elle, l'import CSV fonctionne
# toujours et le message d'erreur renvoie l'école vers ce format.
openpyxl

# Rate limiting
slowapi

# Cache (Redis)
redis

# Queue de tâches asynchrones (Étape F — adossée au Redis déjà présent)
rq

# Tests
pytest
httpx
pytest-asyncio

# Generation PDF
reportlab
